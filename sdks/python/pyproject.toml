[project]
name = "claim169-tests"
version = "0.1.0"
description = "Tests for MOSIP Claim 169 Python SDK"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "cryptography>=42.0",
]
