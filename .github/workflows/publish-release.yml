# Publish release workflow
#
# Triggered when a version tag is pushed (v*).
# Builds and publishes to:
# - crates.io (Rust)
# - PyPI (Python wheels)
# - npm (TypeScript/WASM)
# - Maven Central (Kotlin/Java)
# - GitHub Releases

name: Publish Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  # Extract version metadata
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
      npm_tag: ${{ steps.meta.outputs.npm_tag }}
    steps:
      - name: Extract metadata
        id: meta
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          # Determine if prerelease and npm tag
          if [[ "$VERSION" =~ -alpha ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            echo "npm_tag=alpha" >> $GITHUB_OUTPUT
          elif [[ "$VERSION" =~ -beta ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            echo "npm_tag=beta" >> $GITHUB_OUTPUT
          elif [[ "$VERSION" =~ -rc ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            echo "npm_tag=rc" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
            echo "npm_tag=latest" >> $GITHUB_OUTPUT
          fi

          echo "Version: $VERSION"

  # Run full CI first
  ci:
    name: CI
    uses: ./.github/workflows/ci.yml

  # Build Python wheels
  build-python-wheels:
    name: Build Python Wheels
    needs: [prepare, ci]
    uses: ./.github/workflows/build-python-wheels.yml
    with:
      version: ${{ needs.prepare.outputs.version }}

  # Build TypeScript/WASM package
  build-npm:
    name: Build npm Package
    needs: [prepare, ci]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install wasm-pack
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack@0.13.1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-wasm-release-${{ hashFiles('**/Cargo.lock') }}

      - name: Build WASM
        working-directory: sdks/typescript
        run: |
          npm run build:wasm
          # Create .npmignore to include wasm files (overrides wasm/.gitignore)
          touch wasm/.npmignore

      - name: Install dependencies
        working-directory: sdks/typescript
        run: npm ci

      - name: Build TypeScript
        working-directory: sdks/typescript
        run: npm run build:ts

      - name: Create npm package
        working-directory: sdks/typescript
        run: npm pack

      - name: Upload npm artifact
        uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: sdks/typescript/*.tgz
          if-no-files-found: error

  # Publish to crates.io
  publish-crates:
    name: Publish to crates.io
    needs: [prepare, ci]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-publish-${{ hashFiles('**/Cargo.lock') }}

      - name: Publish claim169-core to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: |
          cargo publish -p claim169-core --no-verify

  # Publish to PyPI using Trusted Publishing (OIDC)
  publish-pypi:
    name: Publish to PyPI
    needs: [build-python-wheels]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/claim169/
    permissions:
      id-token: write
    steps:
      - name: Download wheels
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # No username/password - uses OIDC trusted publishing

  # Publish to npm with provenance
  publish-npm:
    name: Publish to npm
    needs: [prepare, build-npm]
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/claim169
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download npm package
        uses: actions/download-artifact@v4
        with:
          name: npm-package
          path: ./

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: List artifacts
        run: ls -la

      - name: Publish to npm with provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TAG: ${{ needs.prepare.outputs.npm_tag }}
        run: npm publish *.tgz --provenance --access public --tag "$NPM_TAG"

  # Publish to Maven Central (Kotlin/Java SDK)
  publish-maven:
    name: Publish to Maven Central
    needs: [prepare, ci]
    runs-on: ubuntu-latest
    environment:
      name: maven
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Build native library
        run: cargo build --release -p claim169-jni

      - name: Publish to Maven Central
        working-directory: sdks/kotlin
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        run: |
          # Point to release build for publishing
          NATIVE_LIB_DIR="$(pwd)/../../target/release"
          ./gradlew :claim169-core:publishToSonatype closeAndReleaseSonatypeStagingRepository \
            -Djava.library.path="$NATIVE_LIB_DIR" \
            -Djna.library.path="$NATIVE_LIB_DIR"

  # Create GitHub Release
  create-release:
    name: Create GitHub Release
    needs: [prepare, publish-crates, publish-pypi, publish-npm, publish-maven]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install git-cliff
        uses: taiki-e/install-action@v2
        with:
          tool: git-cliff

      - name: Generate release notes
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
        run: |
          git-cliff --tag "v${VERSION}" \
            --strip header \
            --latest \
            -o RELEASE_NOTES.md

          echo ""
          echo "=== Release Notes ==="
          cat RELEASE_NOTES.md

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Prepare release assets
        run: |
          mkdir -p release-assets

          # Collect wheels
          find artifacts -name "*.whl" -exec cp {} release-assets/ \;
          find artifacts -name "*.tar.gz" -exec cp {} release-assets/ \;

          # Collect npm package
          find artifacts -name "*.tgz" -exec cp {} release-assets/ \;

          echo "=== Release assets ==="
          ls -la release-assets/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ needs.prepare.outputs.version }}
          body_path: RELEASE_NOTES.md
          prerelease: ${{ needs.prepare.outputs.is_prerelease == 'true' }}
          files: release-assets/*
          generate_release_notes: false
          fail_on_unmatched_files: true
